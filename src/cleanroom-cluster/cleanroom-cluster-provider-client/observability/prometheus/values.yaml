server:
  extraFlags:
    - web.enable-remote-write-receiver
    - web.enable-lifecycle

  persistentVolume:
    enabled: true
    accessModes:
      - ReadWriteOnce
    size: 20Gi               # Adjust size as needed
    storageClass:
    mountPath: /data         # Default Prometheus data dir

  service:
    clusterIP: None


# Scrape configuration for Prometheus
serverFiles:
  prometheus.yml:
    scrape_configs:
      # Scrape Kubelet metrics from each node
      - job_name: 'kubernetes-kubelet'
        kubernetes_sd_configs:
          - role: node
        scheme: https
        tls_config:
          insecure_skip_verify: true
        bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
        metrics_path: /metrics/resource

      # Scrape cAdvisor metrics for container CPU/memory/fs
      - job_name: 'kubernetes-cadvisor'
        kubernetes_sd_configs:
          - role: node
        scheme: https
        tls_config:
          insecure_skip_verify: true
        bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
        metrics_path: /metrics/cadvisor
        relabel_configs:
          - source_labels: [__meta_kubernetes_node_name]
            action: replace
            target_label: node

      # Scrape DCGM Exporter for GPU hardware metrics (utilization,
      # memory, temperature, power). Now runs as a host systemd service
      # on flex nodes instead of a GPU Operator pod. Uses node discovery
      # to find flex nodes by label and scrapes port 9400 on the node IP.
      # honor_labels preserves DCGM's native pod/namespace/container
      # labels which identify the workload consuming the GPU.
      - job_name: 'dcgm-exporter'
        honor_labels: true
        kubernetes_sd_configs:
          - role: node
        relabel_configs:
          - source_labels: [__meta_kubernetes_node_label_cleanroom_azure_com_flexnode]
            regex: "true"
            action: keep
          - source_labels: [__meta_kubernetes_node_address_InternalIP]
            action: replace
            regex: (.+)
            replacement: $1:9400
            target_label: __address__
          - source_labels: [__meta_kubernetes_node_name]
            action: replace
            target_label: node

      # Scrape GPU health exporter for GPU software-stack readiness
      # metrics (driver, toolkit, CUDA, device-plugin ready states
      # and device count). Now runs as a host systemd service on flex
      # nodes instead of GPU Operator's node-status-exporter pod.
      - job_name: 'gpu-health-exporter'
        scrape_timeout: 30s
        kubernetes_sd_configs:
          - role: node
        relabel_configs:
          - source_labels: [__meta_kubernetes_node_label_cleanroom_azure_com_flexnode]
            regex: "true"
            action: keep
          - source_labels: [__meta_kubernetes_node_address_InternalIP]
            action: replace
            regex: (.+)
            replacement: $1:8000
            target_label: __address__
          - source_labels: [__meta_kubernetes_node_name]
            action: replace
            target_label: node

      - job_name: 'kubernetes-pods'
        honor_labels: true
        kubernetes_sd_configs:
          - role: pod
        relabel_configs:
          - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scrape]
            action: keep
            regex: true
          - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scrape_slow]
            action: drop
            regex: true
          - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scheme]
            action: replace
            regex: (https?)
            target_label: __scheme__
          - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_path]
            action: replace
            target_label: __metrics_path__
            regex: (.+)
          - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_port, __meta_kubernetes_pod_ip]
            action: replace
            regex: (\d+);(([A-Fa-f0-9]{1,4}::?){1,7}[A-Fa-f0-9]{1,4})
            replacement: '[$2]:$1'
            target_label: __address__
          - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_port, __meta_kubernetes_pod_ip]
            action: replace
            regex: (\d+);((([0-9]+?)(\.|$)){4})
            replacement: $2:$1
            target_label: __address__
          - action: labelmap
            regex: __meta_kubernetes_pod_annotation_prometheus_io_param_(.+)
            replacement: __param_$1
          - action: labelmap
            regex: __meta_kubernetes_pod_label_(.+)
          - source_labels: [__meta_kubernetes_namespace]
            action: replace
            target_label: namespace
          - source_labels: [__meta_kubernetes_pod_name]
            action: replace
            target_label: pod
          - source_labels: [__meta_kubernetes_pod_phase]
            regex: Pending|Succeeded|Failed|Completed
            action: drop
          - source_labels: [__meta_kubernetes_pod_node_name]
            action: replace
            target_label: node

      - job_name: prometheus
        static_configs:
          - targets:
            - localhost:9090

prometheus-pushgateway:
  enabled: false

prometheus-node-exporter:
  enabled: false

alertmanager:
  enabled: false

