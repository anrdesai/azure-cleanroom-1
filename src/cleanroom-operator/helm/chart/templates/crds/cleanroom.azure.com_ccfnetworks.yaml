---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: ccfnetworks.cleanroom.azure.com
spec:
  group: cleanroom.azure.com
  names:
    kind: CcfNetwork
    listKind: CcfNetworkList
    plural: ccfnetworks
    shortNames:
    - ccfnet
    singular: ccfnetwork
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.infraType
      name: InfraType
      type: string
    - jsonPath: .status.endpoint
      name: Endpoint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CcfNetwork is the Schema for the ccfnetworks API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CcfNetworkSpec defines the desired state of a CcfNetwork.
            properties:
              deletionPolicy:
                description: |-
                  DeletionPolicy controls whether the external CCF network
                  is deleted or retained when this resource is deleted.
                  When empty, defaults to "retain" (safe).
                enum:
                - retain
                - delete
                type: string
              infraType:
                description: InfraType is the infrastructure type for the CCF network.
                enum:
                - virtual
                - caci
                type: string
                x-kubernetes-validations:
                - message: infraType is immutable once set
                  rule: self == oldSelf
              members:
                description: Members is the list of initial consortium members.
                items:
                  description: MemberSpec defines a CCF consortium member.
                  properties:
                    certificate:
                      description: Certificate is the PEM-encoded member identity
                        certificate.
                      type: string
                    encryptionPublicKey:
                      description: EncryptionPublicKey is the PEM-encoded encryption
                        public key.
                      type: string
                    memberData:
                      description: MemberData is opaque member-specific data.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - certificate
                  type: object
                minItems: 1
                type: array
              nodeCount:
                default: 1
                description: NodeCount is the number of CCF nodes in the network.
                minimum: 1
                type: integer
              nodeLogLevel:
                description: NodeLogLevel sets the CCF node log level.
                enum:
                - Trace
                - Debug
                - Info
                - Fail
                - Fatal
                type: string
              providerConfig:
                description: |-
                  ProviderConfig is opaque provider-specific configuration
                  passed through to the provider client API as-is.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              securityPolicyCreationOption:
                description: |-
                  SecurityPolicyCreationOption controls security
                  policy generation for CACI deployments.
                enum:
                - cached
                - cachedDebug
                - allowAll
                type: string
            required:
            - infraType
            - members
            - nodeCount
            type: object
          status:
            description: CcfNetworkStatus defines the observed state of a CcfNetwork.
            properties:
              conditions:
                description: |-
                  Conditions represent the latest available observations of
                  the CCF network's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              endpoint:
                description: Endpoint is the CCF network endpoint URL.
                type: string
              lastOperationTraceId:
                description: |-
                  LastOperationTraceId is the trace ID of the most recent
                  create operation.
                type: string
              nodes:
                description: Nodes is the list of CCF node identifiers.
                items:
                  type: string
                type: array
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              operationId:
                description: OperationId is the ID of the in-progress async operation.
                type: string
              phase:
                description: Phase is the current lifecycle phase of the CCF network.
                enum:
                - Pending
                - Creating
                - Running
                - Open
                - Failed
                - Deleting
                type: string
              serviceCert:
                description: |-
                  ServiceCert is the PEM-encoded CCF service certificate
                  retrieved from the running network.
                type: string
              traceParent:
                description: |-
                  TraceParent is the W3C traceparent header for the in-flight
                  operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
