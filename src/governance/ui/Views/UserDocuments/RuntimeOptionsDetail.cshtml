@model RuntimeOptionsViewModel
@{
    ViewData["Title"] = "Runtime Options";
}

<div class="content-wrapper">
    <!-- Page Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h2 text-gradient mb-1">
                <i class="fas fa-cogs me-2"></i>Runtime Options
            </h1>
            <p class="text-muted mb-0">Configure runtime settings and execution parameters for user documents</p>
        </div>
        <div class="d-flex gap-2">
            <button type="button" class="btn btn-outline-secondary btn-sm" onclick="refreshOptions()">
                <i class="fas fa-sync-alt me-1"></i>Refresh
            </button>
            <a href="/UserDocuments" class="btn btn-outline-secondary">
                <i class="fas fa-arrow-left me-1"></i>Back to Documents
            </a>
        </div>
    </div>

    <div class="row justify-content-center">
        <!-- Execution Configuration Card -->
        <div class="col-lg-6 mb-4">
            <div class="card h-100">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-play-circle me-2"></i>Execution Control
                    </h5>
                    @if (@Model.Execution.Status == "enabled")
                    {
                        <span class="badge bg-success">
                            <i class="fas fa-check-circle me-1"></i>Enabled
                        </span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">
                            <i class="fas fa-pause-circle me-1"></i>Disabled
                        </span>
                    }
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <small class="text-muted">Current Status:</small>
                        <div class="mt-1">
                            <code class="bg-light p-2 rounded d-block">@Model.Execution.Status</code>
                        </div>
                    </div>

                    @if (@Model.Execution.Status == "disabled" && @Model.Execution.Reason?.Message != null)
                    {
                        <div class="alert alert-warning d-flex align-items-start mb-3">
                            <i class="fas fa-exclamation-triangle me-2 mt-1"></i>
                            <div>
                                <small class="fw-semibold">Execution Disabled</small>
                                <div class="mt-1">
                                    <small>@Model.Execution.Reason.Message</small>
                                </div>
                            </div>
                        </div>
                    }

                    <div class="mb-3">
                        <small class="text-muted">Description:</small>
                        <p class="small mt-1 mb-0">Control whether user documents can be executed within the clean room environment. When disabled, documents remain accessible but cannot be run.</p>
                    </div>

                    <div class="d-grid">
                        @if (@Model.Execution.Status == "enabled")
                        {
                            <a asp-controller="UserDocuments" asp-action="DisableExecution" asp-route-id="@System.Net.WebUtility.HtmlEncode(Model.Id)"
                               class="btn btn-outline-danger">
                                <i class="fas fa-stop me-1"></i>Disable Execution
                            </a>
                        }
                        else if (@Model.Execution.Status == "disabled")
                        {
                            <a asp-controller="UserDocuments" asp-action="EnableExecution" asp-route-id="@System.Net.WebUtility.HtmlEncode(Model.Id)"
                               class="btn btn-outline-success">
                                <i class="fas fa-play me-1"></i>Enable Execution
                            </a>
                        }
                    </div>
                </div>
            </div>
        </div>

        <!-- Telemetry Configuration Card -->
        <div class="col-lg-6 mb-4">
            <div class="card h-100">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-chart-line me-2"></i>Telemetry Collection
                    </h5>
                    @if (@Model.Telemetry.Status == "enabled")
                    {
                        <span class="badge bg-success">
                            <i class="fas fa-check-circle me-1"></i>Enabled
                        </span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">
                            <i class="fas fa-pause-circle me-1"></i>Disabled
                        </span>
                    }
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <small class="text-muted">Current Status:</small>
                        <div class="mt-1">
                            <code class="bg-light p-2 rounded d-block">@Model.Telemetry.Status</code>
                        </div>
                    </div>

                    @if (@Model.Telemetry.Status == "disabled" && @Model.Telemetry.Reason?.Message != null)
                    {
                        <div class="alert alert-warning d-flex align-items-start mb-3">
                            <i class="fas fa-exclamation-triangle me-2 mt-1"></i>
                            <div>
                                <small class="fw-semibold">Telemetry Disabled</small>
                                <div class="mt-1">
                                    <small>@Model.Telemetry.Reason.Message</small>
                                </div>
                            </div>
                        </div>
                    }

                    <div class="mb-3">
                        <small class="text-muted">Description:</small>
                        <p class="small mt-1 mb-0">Enable or disable telemetry collection for user document operations. Telemetry helps monitor performance and usage patterns.</p>
                    </div>

                    <div class="d-grid">
                        @if (@Model.Telemetry.Status == "enabled")
                        {
                            <a asp-controller="UserDocuments" asp-action="DisableTelemetry" asp-route-id="@System.Net.WebUtility.HtmlEncode(Model.Id)"
                               class="btn btn-outline-warning">
                                <i class="fas fa-pause me-1"></i>Disable Telemetry
                            </a>
                        }
                        else if (@Model.Telemetry.Status == "disabled")
                        {
                            <a asp-controller="UserDocuments" asp-action="EnableTelemetry" asp-route-id="@System.Net.WebUtility.HtmlEncode(Model.Id)"
                               class="btn btn-outline-success">
                                <i class="fas fa-play me-1"></i>Enable Telemetry
                            </a>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Information Section -->
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h6 class="card-title mb-0">
                        <i class="fas fa-info-circle me-2"></i>Runtime Options Information
                    </h6>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <h6 class="text-primary mb-2">
                                <i class="fas fa-play-circle me-1"></i>Execution Control
                            </h6>
                            <ul class="list-unstyled mb-3">
                                <li class="mb-1 d-flex">
                                    <i class="fas fa-check text-success me-2 mt-1"></i>
                                    <small>Controls document execution permissions</small>
                                </li>
                                <li class="mb-1 d-flex">
                                    <i class="fas fa-check text-success me-2 mt-1"></i>
                                    <small>Immediate effect when changed</small>
                                </li>
                                <li class="d-flex">
                                    <i class="fas fa-check text-success me-2 mt-1"></i>
                                    <small>Maintains document accessibility</small>
                                </li>
                            </ul>
                        </div>
                        <div class="col-md-6">
                            <h6 class="text-primary mb-2">
                                <i class="fas fa-chart-line me-1"></i>Telemetry Collection
                            </h6>
                            <ul class="list-unstyled mb-0">
                                <li class="mb-1 d-flex">
                                    <i class="fas fa-check text-success me-2 mt-1"></i>
                                    <small>Monitors performance metrics</small>
                                </li>
                                <li class="mb-1 d-flex">
                                    <i class="fas fa-check text-success me-2 mt-1"></i>
                                    <small>Tracks usage patterns</small>
                                </li>
                                <li class="d-flex">
                                    <i class="fas fa-check text-success me-2 mt-1"></i>
                                    <small>Privacy-compliant data collection</small>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    function refreshOptions() {
        const refreshBtn = document.querySelector('[onclick="refreshOptions()"]');
        const icon = refreshBtn.querySelector('i');

        icon.classList.add('fa-spin');
        refreshBtn.disabled = true;

        setTimeout(() => {
            icon.classList.remove('fa-spin');
            refreshBtn.disabled = false;
            location.reload();
        }, 1000);
    }
</script>

<style>
    .card {
        transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
    }

    .card:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .text-gradient {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }
</style>
