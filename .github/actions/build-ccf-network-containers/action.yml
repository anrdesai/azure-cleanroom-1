name: Build CCF network containers
description: Build CCF network containers

inputs:
  tag:
    required: true
    description: The tag

runs:
  using: composite
  steps:
    - uses: ./.github/actions/build-container-and-artefacts
      with:
        name: ccf-app-run-js-snp
        tag: ${{ inputs.tag }}
        dockerfile: build/docker/Dockerfile.ccf_run_js_app
        context: ${{ github.workspace }}

    - uses: ./.github/actions/build-container-and-artefacts
      with:
        name: ccf-app-run-js-virtual
        tag: ${{ inputs.tag }}
        dockerfile: build/docker/Dockerfile.ccf_run_js_app
        context: ${{ github.workspace }}

    - uses: ./.github/actions/build-container-and-artefacts
      with:
        name: ccr-proxy
        tag: ${{ inputs.tag }}
        dockerfile: build/docker/Dockerfile.proxy
        context: ${{ github.workspace }}

    - uses: ./.github/actions/build-container-and-artefacts
      with:
        name: skr
        tag: ${{ inputs.tag }}
        dockerfile: build/docker/Dockerfile.skr
        context: ${{ github.workspace }}

    - uses: ./.github/actions/build-container-and-artefacts
      with:
        name: ccf-recovery-agent
        tag: ${{ inputs.tag }}
        dockerfile: build/docker/Dockerfile.ccf-recovery-agent
        context: ${{ github.workspace }}

    - uses: ./.github/actions/build-container-and-artefacts
      with:
        name: cvm-attestation-verifier
        tag: ${{ inputs.tag }}
        dockerfile: build/docker/Dockerfile.cvm-attestation-verifier
        context: ${{ github.workspace }}