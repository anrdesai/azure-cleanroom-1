FROM cleanroombuild.azurecr.io/mirror/docker/library/python:3.12 AS wheel-builder

ARG UV_DEFAULT_INDEX=https://pypi.org/simple
ENV UV_DEFAULT_INDEX=$UV_DEFAULT_INDEX

WORKDIR /build

RUN apt-get update && apt-get install -y curl build-essential && rm -rf /var/lib/apt/lists/*
RUN curl -LsSf https://astral.sh/uv/install.sh | sh
ENV PATH="/root/.local/bin:$PATH"

COPY src/s3fs-launcher src/s3fs-launcher
COPY src/internal/cleanroom-internal src/internal/cleanroom-internal
COPY pyproject.toml .
COPY uv.lock .
RUN uv lock && uv sync --frozen

RUN uv build --all-packages --wheel

FROM cleanroombuild.azurecr.io/mirror/docker/library/python:3.12

ARG PIP_INDEX_URL=https://pypi.org/simple
ENV PIP_INDEX_URL=$PIP_INDEX_URL

# Install dependencies.
RUN apt-get update -y && \
    apt-get -y --no-install-recommends install \
    s3fs \
    curl

COPY src/scripts/wait-for-it.sh ./wait-for-it.sh
RUN chmod +x ./wait-for-it.sh

WORKDIR /setup

COPY --from=wheel-builder /build/dist/cleanroom_internal*.whl /setup/
COPY --from=wheel-builder /build/dist/s3fs_launcher*.whl /setup/

RUN pip install *.whl && rm -f *.whl